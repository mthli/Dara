<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/keywords"
          android:title="@string/menu_filter_with_keywords"
          android:icon="@drawable/ic_keywords"
          app:showAsAction="never">
    </item>

    <item android:id="@+id/time"
          android:title="@string/menu_filter_with_time"
          android:icon="@drawable/ic_time"
          app:showAsAction="never">
    </item>

    <item android:id="@+id/custom"
          android:title="@string/menu_custom_reminder"
          android:icon="@drawable/ic_palette"
          app:showAsAction="never">
    </item>

    <!-- TODO more functions -->

    <item android:id="@+id/space"
          android:title="@null"
          app:showAsAction="never">
    </item>

</menu>